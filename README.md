# Python Terminal Chat Experiment

A small command-line exercise for entering and displaying messages in one local terminal session. It does not call an AI model or connect multiple users.

## Run

On Windows, run `python Chat.py`, enter a name, and type messages. Enter `fim` to exit. The script uses the Windows `cls` command to clear the screen and keeps messages only in memory.
